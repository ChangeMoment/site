[Unit]
Description=Watch for ChangeMoment WordPress publish requests

[Path]
PathChanged=/var/lib/changemoment/rebuild-requested
Unit=changemoment-rebuild.service

[Install]
WantedBy=multi-user.target
